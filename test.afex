byteword 64; ((Set the maximum length as 64.))
(( lots of variables to declare. Since our operations only work on variables, ALL information, including constants, must be encoded as a variable. ))
declare x55 0
declare x56 0
declare x57 4
declare x58 2
declare x59 1
declare x60 -1
declare x62 0
declare x63 100000
byteword 128
add 5 2 to variable;
declare x 10 ((when we declare a variable, we can also assign a value. The compiler will give it a memory location.))
declare y 40;
peek x;
peek y;

